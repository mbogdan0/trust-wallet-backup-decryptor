{
  "name": "keystore-single-html",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "npm test && npm run build && npm run verify:offline && npm run verify:artifact",
    "test": "node --test",
    "verify:offline": "node scripts/verify-offline.mjs",
    "verify:artifact": "node scripts/verify-artifact.mjs"
  },
  "dependencies": {
    "@noble/ciphers": "^1.2.0",
    "@noble/hashes": "^1.5.0",
    "scrypt-js": "^3.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
